# PowerShell 5.1 читает .ps1 как ANSI, если нет UTF-8 BOM — кириллица в комментариях
# и выводе рассыпается. BOM хранится в файле; здесь фиксируем перевод строк в CRLF,
# чтобы Windows-хосты не спотыкались на смешанных окончаниях.
*.ps1 text eol=crlf

*.sh  text eol=lf
*.md  text
